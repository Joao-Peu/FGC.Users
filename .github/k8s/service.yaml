apiVersion: v1
kind: Service
metadata:
  name: fgc-users-svc
  namespace: fcg
  labels:
    app: fgc-users
spec:
  selector:
    app: fgc-users
  ports:
    - name: http
      protocol: TCP
      port: 80
      targetPort: 8080
  type: ClusterIP
